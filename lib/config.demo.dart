// TODO: rename me to `config.dart` and fill up your secret information

// loginInfo for testing - `test/db.dart`
final loginInfo = {
  'user': null,
  'password': null,
};

// TODO:
// change to `mongodb://user:password@your_server:port/db`
const MONGO_DB_URL = null;
